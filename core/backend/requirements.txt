# NeuroSync — Core Dependencies
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
numpy>=1.24.0

# NLP / ML
sentence-transformers>=2.2.0
spacy>=3.7.0

# Fallback semantic
scikit-learn>=1.3.0

# Persistence (blueprint 09, doc 15 §4) — PostgreSQL in production.
# SQLite needs no driver: it ships with Python, and is what local development
# and tools/state_probe.py run against.
sqlalchemy>=2.0.0
alembic>=1.13.0
psycopg[binary]>=3.1.0
